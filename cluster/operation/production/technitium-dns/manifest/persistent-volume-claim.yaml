apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: technitium-data
  namespace: technitium-dns
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: longhorn
  resources:
    requests:
      storage: 2Gi
