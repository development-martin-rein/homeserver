apiVersion: v1
kind: Service
metadata:
  name: technitium-web
  namespace: technitium-dns
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: technitium
  ports:
    - name: web
      port: 5380
      targetPort: 5380
      protocol: TCP
