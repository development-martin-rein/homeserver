apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: technitium-dns
  namespace: argocd
spec:
  project: homeserver

  source:
    repoURL: https://github.com/development-martin-rein/homeserver.git
    targetRevision: main
    path: cluster/operation/production/technitium-dns/manifest

  destination:
    server: https://kubernetes.default.svc
    namespace: technitium-dns

  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
